---
module_name: sonic_pki
tests:
  - name: PKI Merged Test
    hosts: datacenter
    gather_facts: false
    connection: httpapi
    collections:
      - dellemc.enterprise_sonic
    config:
      trust-stores:
        - name: a
    state: merged
